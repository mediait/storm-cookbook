########### These MUST be filled in for a storm configuration
storm.zookeeper.servers:
<% node[:stormproject][:supervisor][:hosts].each do |host| %>
- "<%= host %>"
<% end %>

storm.local.dir: "/home/<%= node[:deployment][:user] %>/storm-data"
java.library.path: "/usr/local/lib"

topology.debug: true

nimbus.host: "<%= node[:stormproject][:nimbus][:host] %>"
nimbus.task.launch.secs : 240
nimbus.childopts: "<%= node[:stormproject][:nimbus][:childopts] %>"
ui.childopts: "<%= node[:stormproject][:ui][:childopts] %>"
worker.childopts: "<%= node[:stormproject][:worker][:childopts] %>"

supervisor.worker.start.timeout.secs: 240
supervisor.worker.timeout.secs: 240
supervisor.childopts: "<%= node[:stormproject][:supervisor][:childopts] %>"
supervisor.slots.ports:
<% node[:stormproject][:supervisor][:workerports].each do |port| %>
  - <%= port %>
<% end %>
