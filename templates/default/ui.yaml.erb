########### These MUST be filled in for a storm configuration
storm.zookeeper.servers:
<% node[:stormproject][:supervisor][:hosts].each do |host| %>
- "<%= host %>"
<% end %>

storm.local.dir: "/home/<%= node[:deployment][:user] %>/storm-data"
java.library.path: "/usr/local/lib"

nimbus.host: "<%= node[:stormproject][:nimbus][:host] %>"

ui.childopts: "<%= node[:stormproject][:ui][:childopts] %>"
